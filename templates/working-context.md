# Working Context Template

只在长任务的语义状态变化、上下文压缩或交接时使用；压缩、恢复、模型/目录变化和网络/传输失败由连续性守卫自动触发复核：

```text
目标:
约束与已确认事实:
已改文件:
验证状态:
doNotRetry:
唯一下一步:
```

恢复时先用当前用户请求、`git status`、`git diff`、目标文件、运行中工具和外部操作回执复核；不要保存凭证、客户数据、大段日志、外部 prompt 或未验证猜测。
