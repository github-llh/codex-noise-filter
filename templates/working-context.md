# Working Context Template

只在长任务的语义状态变化、上下文压缩或交接时使用；压缩、恢复、模型/目录变化和网络/传输失败由连续性守卫自动触发复核：

```text
目标与验收条件:
不可违反约束:
已确认事实与关键决策:
已改文件:
验证状态:
运行中工具与外部操作幂等性:
doNotRetry:
唯一下一步:
```

压缩前确保摘要保留上述字段；恢复时先用当前用户请求、`git status`、`git diff`、目标文件、运行中工具和外部操作回执复核。没有证据的字段标记为未确认；不要保存凭证、客户数据、大段日志、外部 prompt 或未验证猜测。
