interface:
  display_name: "codex-noise-filter"
  short_description: "以证据收敛编程任务，并在中断后自动恢复连续性。"
  brand_color: "#2563EB"
  default_prompt: "使用 codex-noise-filter 判断任务模式，收敛证据与触碰范围；压缩、恢复、模型变化或网络失败后自动复核落盘状态，再完成与风险匹配的验证。"

policy:
  allow_implicit_invocation: true
